apiVersion: v1
kind: Service
metadata:
  name: {{ include "assistserver.fullname" . }}
  labels:
    {{- include "assistserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "assistserver.selectorLabels" . | nindent 4 }}
