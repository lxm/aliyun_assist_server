{
    "db": {
        "host": "${DB_HOST:127.0.0.1}",
        "port": "${DB_PORT:3306}",
        "database": "${DB_DATABASE:aliyun_assist}",
        "username": "${DB_USERNAME:root}",
        "password": "${DB_PASSWORD:123456}"
    },
    "redis": {
        "host": "${REDIS_HOST:127.0.0.1}",
        "port": "${REDIS_PORT:6379}",
        "database": "${REDIS_DB:0}",
        "password": "${REDIS_PASSWORD:}"
    }
}